spring:
  batch:
    jdbc:
      initialize-schema: ALWAYS
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/batchdemo
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    job: 
      enabled: true
    
