//Dom is a structure of HTML elements which are converted into objects(Nodes) and arrange all this object in tree data structure .

//The HTML page in the browser will  convert into bytes->characters->tokens->Objects(Nodes)
//The browser engine will link all the nodes in the sequence and all nodes will be placed in tree data structure and that is called DOM 